#' Example count data taken from a real experiment but anonimized trough random
#' jittering of the counts.
#' 
#' The dataset has samples as columns and genes as rows. 
"count_data"

#' Example metadata of the count data from a real experiment but anonimized.
#' Each column is a different experimental condition and each row is a column
#' in the count data.
"count_metadata"